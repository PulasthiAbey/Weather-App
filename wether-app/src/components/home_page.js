import React from "react";
import { View } from "react-native";
import { ScrollView } from "react-native-gesture-handler";

import stylesComponent from "../tools/landingStyles";

const home_page = () => {
  return (
    <ScrollView>
      {/* View of the API */}
      <View></View>
    </ScrollView>
  );
};

export default home_page;
